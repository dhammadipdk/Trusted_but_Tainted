# Delta Embeddings

This directory stores delta embeddings used for Differential Morphing Attack Detection (D-MAD).

Delta embeddings are generated from the embeddings stored in:

Embeddings/

and are consumed by:

05_dmad_evaluation.ipynb

Precomputed delta embeddings for the complete experimental setup are available through the Artifact Downloads section of the repository.
